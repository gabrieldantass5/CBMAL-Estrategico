# ⌨️ Playbook: Maestria no Terminal e Vibe Coding

Este guia prático foi criado para transformar o Tenente em um **Power User** de terminal, focado em ferramentas de IA (Claude Code e Gemini CLI).

---

## 🏗️ 1. A Filosofia do "Vibe Coding"

Vibe Coding não é apenas digitar comandos; é **conduzir a IA**.

1. **Intenção sobre Implementação**: Diga *o quê*, não *como*.
2. **Contexto é Rei**: Use o terminal para dar visibilidade total à IA (ex: `ls -R`, `cat file.md`).
3. **Ciclo de Feedback Curto**: Execute, veja o erro, peça correção. Sem medo.

---

## 🛠️ 2. Comandos Essenciais do Terminal (Windows PowerShell)

Para dominar a IA, você precisa saber navegar:

| Comando | O que faz? | Por que usar no Vibe Coding? |
| :--- | :--- | :--- |
| `ls` (ou `dir`) | Lista arquivos da pasta atual | Para mostrar à IA o que temos disponível. |
| `cd nome_da_pasta` | Entra em uma pasta | Para focar o contexto em um projeto específico. |
| `cat arquivo.md` | Mostra conteúdo do arquivo | Para você ler rapidamente o que a IA gerou. |
| `mkdir nome` | Cria uma nova pasta | Para organizar novos módulos de estudo. |
| `clear` (ou `cls`) | Limpa a tela | Para focar no que importa agora. |

---

## 🦅 3. Claude Code: Comandos de Ouro (Aulas 14-16)

O Claude Code (`claude`) vive dentro do seu terminal. Comandos principais:

- `/help`: Abre o guia de socorro.
- `/compact`: **CRÍTICO!** Resume a conversa para economizar tokens e manter a IA "inteligente" (limpa a memória de curto prazo).
- `/clear`: Reinicia a conversa (contexto limpo).
- `/cost`: Mostra quanto custou a brincadeira em USD.
- `/files`: Lista os arquivos que o Claude está "vendo" no momento.

---

## ♊ 4. Gemini CLI (Antigravity Context)

Como estamos usando o **Antigravity** (baseado em Gemini), sua interface é este chat. Mas você pode "invocar" poderes via scripts:

- `python scripts/kimi_thinker.py`: Aciona o "modo de pensamento profundo" para problemas complexos de lógica militar.
- `python scripts/doc_converter.py`: Converte seus rascunhos em documentos oficiais.

---

## 🎯 Desafio de Hoje: "The Navigator"

Tente fazer o seguinte no seu terminal (Claude Code):

1. Use `ls` para ver as pastas.
2. Use `cd 09_Evolucao_e_Aprendizado` para entrar aqui.
3. Use `/compact` se a conversa estiver longa.
4. Peça ao Claude: *"Crie um script simples em python que diga 'Vibe Coding Ativado!' e execute-o."*

---
*Última atualização: 30/01/2026*
